#!/usr/bin/octave -qf

% includes

tardis;

% output

disp ('');

% latinBass (Rod(1));

% sesquialterum (Rod(2));

% hexGuitar (Rod(3));

 sesquialterum (Rod(4));

disp ('');

% cleanup

clear Rod;

